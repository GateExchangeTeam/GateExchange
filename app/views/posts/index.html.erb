<div>
<h1> Posts </h1>
<h2><%= link_to "Ask a Question", new_course_post_path(@id) %></h2>
 <% @posts.each do |p| %>
   <h2><%= link_to p.title, course_post_comments_path(@id, p.id) %></h2>
    <p><%= p.description %><p>
    <p><%= p.view %><p>
 <% end %>
</div>